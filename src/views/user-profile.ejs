<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body { font-family: Arial, sans-serif; background: #f8fafc; margin: 0; padding: 0; }
    .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 12px; box-shadow: 0 2px 16px rgba(0,0,0,0.07); padding: 40px; }
    h1 { color: #2d7a46; margin-bottom: 16px; text-align: center; }
    p { color: #333; font-size: 1.1rem; line-height: 1.6; margin-bottom: 20px; text-align: center; }
    .nav { text-align: center; margin-bottom: 30px; }
    .nav a { color: #2d7a46; text-decoration: none; margin: 0 15px; }
    .nav a:hover { text-decoration: underline; }
    .profile-card { background: #f1f5f9; padding: 30px; border-radius: 8px; border-left: 4px solid #2d7a46; }
    .profile-field { margin-bottom: 15px; }
    .field-label { font-weight: bold; color: #2d7a46; margin-bottom: 5px; }
    .field-value { color: #333; font-size: 1.1rem; }
    .role-badge { display: inline-block; background: #2d7a46; color: white; padding: 4px 12px; border-radius: 20px; font-size: 0.9rem; }
  </style>
</head>
<body>
  <div class="container">
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <a href="/users">Users</a>
      <a href="/dashboard">Dashboard</a>
    </div>
    
    <h1><%= title %></h1>
    <p><%= message %></p>
    
    <div class="profile-card">
      <div class="profile-field">
        <div class="field-label">Name:</div>
        <div class="field-value"><%= user.name || 'No Name' %></div>
      </div>
      
      <div class="profile-field">
        <div class="field-label">Email:</div>
        <div class="field-value"><%= user.email %></div>
      </div>
      
      <div class="profile-field">
        <div class="field-label">Role:</div>
        <div class="field-value">
          <span class="role-badge"><%= user.role %></span>
        </div>
      </div>
      
      <div class="profile-field">
        <div class="field-label">User ID:</div>
        <div class="field-value"><%= user.id %></div>
      </div>
      
      <div class="profile-field">
        <div class="field-label">Joined:</div>
        <div class="field-value"><%= new Date(user.createdAt).toLocaleString() %></div>
      </div>
    </div>
    
    <div style="text-align: center; margin-top: 20px;">
      <a href="/users" style="color: #2d7a46; text-decoration: none;">← Back to Users</a>
    </div>
  </div>
</body>
</html> 