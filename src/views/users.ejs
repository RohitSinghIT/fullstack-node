<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body { font-family: Arial, sans-serif; background: #f8fafc; margin: 0; padding: 0; }
    .container { max-width: 1000px; margin: 40px auto; background: #fff; border-radius: 12px; box-shadow: 0 2px 16px rgba(0,0,0,0.07); padding: 40px; }
    h1 { color: #2d7a46; margin-bottom: 16px; text-align: center; }
    p { color: #333; font-size: 1.1rem; line-height: 1.6; margin-bottom: 20px; text-align: center; }
    .nav { text-align: center; margin-bottom: 30px; }
    .nav a { color: #2d7a46; text-decoration: none; margin: 0 15px; }
    .nav a:hover { text-decoration: underline; }
    .users-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; margin-top: 30px; }
    .user-card { background: #f1f5f9; padding: 20px; border-radius: 8px; border-left: 4px solid #2d7a46; }
    .user-name { font-weight: bold; color: #2d7a46; margin-bottom: 8px; }
    .user-email { color: #666; margin-bottom: 5px; }
    .user-role { color: #888; font-size: 0.9rem; }
    .user-date { color: #999; font-size: 0.8rem; margin-top: 10px; }
    .stats { background: #e8f5e8; padding: 15px; border-radius: 8px; text-align: center; margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <a href="/users">Users</a>
      <a href="/dashboard">Dashboard</a>
    </div>
    
    <h1><%= title %></h1>
    <p><%= message %></p>
    
    <div class="stats">
      <strong>Total Users: <%= users.length %></strong>
    </div>

    <div class="users-grid">
      <% users.forEach(user => { %>
        <div class="user-card">
          <div class="user-name"><%= user.name || 'No Name' %></div>
          <div class="user-email"><%= user.email %></div>
          <div class="user-role">Role: <%= user.role %></div>
          <div class="user-date">Joined: <%= new Date(user.createdAt).toLocaleDateString() %></div>
          <a href="/user/<%= user.id %>" style="color: #2d7a46; text-decoration: none; font-size: 0.9rem;">View Profile →</a>
        </div>
      <% }); %>
    </div>
  </div>
</body>
</html> 