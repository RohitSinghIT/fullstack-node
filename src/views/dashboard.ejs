<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body { font-family: Arial, sans-serif; background: #f8fafc; margin: 0; padding: 0; }
    .container { max-width: 1000px; margin: 40px auto; background: #fff; border-radius: 12px; box-shadow: 0 2px 16px rgba(0,0,0,0.07); padding: 40px; }
    h1 { color: #2d7a46; margin-bottom: 16px; text-align: center; }
    p { color: #333; font-size: 1.1rem; line-height: 1.6; margin-bottom: 20px; text-align: center; }
    .nav { text-align: center; margin-bottom: 30px; }
    .nav a { color: #2d7a46; text-decoration: none; margin: 0 15px; }
    .nav a:hover { text-decoration: underline; }
    .dashboard-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-top: 30px; }
    .stat-card { background: #f1f5f9; padding: 25px; border-radius: 8px; text-align: center; border-left: 4px solid #2d7a46; }
    .stat-number { font-size: 2.5rem; font-weight: bold; color: #2d7a46; margin-bottom: 10px; }
    .stat-label { color: #666; font-size: 1rem; }
    .user-info { background: #e8f5e8; padding: 25px; border-radius: 8px; margin-bottom: 30px; }
    .welcome-text { font-size: 1.2rem; color: #2d7a46; margin-bottom: 15px; }
    .user-details { color: #333; }
  </style>
</head>
<body>
  <div class="container">
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <a href="/users">Users</a>
      <a href="/dashboard">Dashboard</a>
    </div>
    
    <h1><%= title %></h1>
    <p><%= message %></p>
    
    <div class="user-info">
      <div class="welcome-text">Welcome, <%= currentUser.name || currentUser.email %>!</div>
      <div class="user-details">
        <strong>Email:</strong> <%= currentUser.email %> | 
        <strong>Role:</strong> <%= currentUser.role %>
      </div>
    </div>

    <div class="dashboard-grid">
      <div class="stat-card">
        <div class="stat-number"><%= stats.totalUsers %></div>
        <div class="stat-label">Total Users</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-number"><%= stats.adminUsers %></div>
        <div class="stat-label">Admin Users</div>
      </div>
      
      <div class="stat-card">
        <div class="stat-number"><%= stats.customerUsers %></div>
        <div class="stat-label">Customer Users</div>
      </div>
    </div>
    
    <div style="text-align: center; margin-top: 30px;">
      <a href="/users" style="color: #2d7a46; text-decoration: none; margin-right: 20px;">View All Users</a>
      <a href="/graphql" style="color: #2d7a46; text-decoration: none;">GraphQL Playground</a>
    </div>
  </div>
</body>
</html> 